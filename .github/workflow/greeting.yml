name: Greeting Workflow

on:
  workflow_dispatch:

jobs:
  greet:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Output Greeting
        run: echo "Hello, welcome to the GitHub Actions workflow!"